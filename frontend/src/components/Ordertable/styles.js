import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    table: {
        minWidth: 650,
    },
    tableCell: {
        padding: '8px',
        borderRight: '1px solid #e0e0e0', 
    },
}));